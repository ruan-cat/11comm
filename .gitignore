# Logs
# 忽略日志文件夹 避免出现重名的业务组件被减少提交
# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
.output
coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

# 不提交仓库中的依赖包
node_modules

# 不提供自动生成的类型文件
auto-imports.d.ts
typed-router.d.ts
route-map.d.ts

# 满足特定命名规则的文件，认定为自动生成的类型文件，不予提交
components.d.ts
components-instance.d.ts
# 名称多一个短横线的文件，认定为主动生成的类型文件，不予提交
components-*.ts

# 我们自动生成工作区文件 所以不提交到仓库内
*.code-workspace

# 忽略自动生成的依赖锁文件 不提供包管理器的依赖锁文件
package-lock.json
!examples/gitee-exampl-app/package-lock.json

# 忽略自动生成vercel部署配置文件
.vercel
vercel.null.def.json

# 忽略生成的构建缓存
.vite

# 忽略vite的中间构建文件 防止其他人误提交此文件
vite.config.ts.*.mjs

# 单独的环境变量文件 要忽略 因为存储了敏感信息
# !.env
.env
# 忽略掉自定义的环境变量
.env.*
# 忽略掉来自vercel获取的数据库环境变量文件
.env*.local

# 环境变量文件 提交
!.env.development
!.env.production
# Vercel 项目配置环境变量 可以提交到 Git
!.env.vercel

.vercel

# 忽略 vitepress 项目生成的缓存
*/**/.vitepress/dist
*/**/.vitepress/cache

# 忽略typedoc生成的md文件
/src/docs/typedoc-md

# 忽略被移动到此处的变更集文件
/src/CHANGELOG.md

# 忽略turbo任务调度产生的缓存目录
.turbo

# 忽略wrangler部署配置文件
.wrangler

# 忽略本地缓存目录
local_cache

# 忽略 Superpowers 本地工作目录
.superpowers/
