{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build:h5:prod": {
      "cache": true,
      "outputs": ["dist/**"]
    },
    "docs:build": {
      "cache": true,
      "outputs": ["docs/.vitepress/dist/**"]
    }
  }
}
