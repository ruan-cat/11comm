/**
 * @file Drizzle ORM Schema 定义
 * @description 在此文件中定义数据库表结构
 *
 * 常用类型导入示例：
 * import { integer, pgTable, serial, text, timestamp, boolean, varchar } from "drizzle-orm/pg-core";
 *
 * 表定义示例：
 * export const exampleTable = pgTable("example", {
 *   id: serial("id").primaryKey(),
 *   name: text("name").notNull(),
 *   createdAt: timestamp("created_at").notNull().defaultNow(),
 * });
 *
 * 类型推导示例：
 * export type InsertExample = typeof exampleTable.$inferInsert;
 * export type SelectExample = typeof exampleTable.$inferSelect;
 */

// TODO: 在此处添加业务表定义
