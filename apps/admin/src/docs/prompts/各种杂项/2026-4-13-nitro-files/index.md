<!-- TODO: 长任务，未开始 -->

# 2026-4-13 用 nitro+neon 数据库，实现简单的大文件上传需求

我需要实现前端层面的面试，面试题经常有这样的问题： 如何设计并实现前端层面的大文件分片上传、断点续传？并确保大文件可以安全可靠的上传？

我需要让现在的这个 admin 项目，在一个合适的地方，实现文件大文件上传任务，然后务必体现出前端能力，便于我面试说清楚技术细节。

本次业务实现的最终目的是为了帮助我学习，备考，大文件上传的技术。便于我积累和面试官的沟通谈资。

## 具体的前端断点续传实现技术

- https://www.mianshiya.com/question/1824744530633592833

如果你有更好的，更加合适的，纯前端层面能恰当实现的断点续传技术设计，也可以和我沟通。但是我认为现在的分片上传和断点续传，基本上是一个固定的做法了。我允许你通过服用现有的包，第三方前端包来实现断点续传，但是我还是很希望你手动实现，因为我凭感觉得知，断点续传是一个很依赖后端支持的需求。

## 调研并设计一个轻量化的，简单的后端实现

我的核心目的永远是前端面试，前端求职。所以关于 nitro 接口 + neon 数据库的`后端层面`的断点续传设计和代码实现，我是不关心的，这是配套需要实现的。你务必追求核心的断点续传后端实现，确保能够在 nitro 接口 + neon 数据库的`后端层面`上，能用就行。我不关心后端的东西，我只是需要一个后端支持我，用于更好的表达出前端技能。

## 恰当的同步修改数据库表，增加合适的业务字段

毕竟我需要演示清楚这个断点续传的前端能力和技术，要落实到一个具体的前端业务内。你可以调研一下现在的 admin 项目，只需要找到一个关键的大文件上传场景即可，在这个大文件场景内，增加业务字段，增加数据库 schema 的数据库字段。

## 对应业务演示场景补充数据新增、修改、维护的接口

关于文件上传的业务，这个业务是闭环的。对应的接口也要补充实现到位。这是后续需要逐步补充的内容。你至少要确保前端层面有 2 个接口，一个是新增、另一个是编辑的接口，能够实现文件上传，与修改。

先完成最核心的接口开发，实现前端的接口对接，再开始补齐，完成业务闭环所对应的接口。

因为我太清楚了，我们项目大部分的地方，都没有完整的 CURD 接口。

- 没有新增、编辑、和查看的 nitro 接口开发出来。
- 也没有充分使用 zod 的数据校验来开发新增和编辑接口。
- 也没有对应的 vue-query 接口封装好。

这些都是在完成闭环式演示业务场景所需要同步补齐的对应基础设施。这些是完成核心前端断点续传业务后，所需要补齐的内容。

## 和我仔细沟通协商

这个任务需求有很多细节没有细化清楚，我只是说明的大概方向。请你使用 superpower 系列的技能，和我沟通细化这个最小化的落地。确保我能够有核心谈资去面对这个常见的面试问题————`如何设计并实现前端层面的大文件分片上传、断点续传？并确保大文件可以安全可靠的上传？`
