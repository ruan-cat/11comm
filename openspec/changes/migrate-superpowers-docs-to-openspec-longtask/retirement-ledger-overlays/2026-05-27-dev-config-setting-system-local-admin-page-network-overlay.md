# 2026-05-27 dev config 与 setting system 本地页面 Network overlay

本 overlay 只补 `dev-team/config-manage` 四个 list 与 `setting-manage/system-manage` 五个 list 的本地 admin H5 页面 Network 投影。证据 artifact 为 `.tmp/phase7-dev-browser/2026-05-27-task540-dev-config-setting-system-local-admin-network-9of9.md`。

采证环境：本地 `apps/api` 为 `http://127.0.0.1:3102`，本地 `apps/admin` 为 `http://127.0.0.1:8084`，admin 页面通过 `/api-shadow` proxy 转发到本地统一 `apps/api`。本 overlay 仅把对应行的 `browserEvidence` 从 pending 升级为本地页面 Network partial；所有行的 `retirementDecision` 仍为 `keep-source`，`deleteCandidate=false`。

关闭边界：本 overlay 不是生产 admin H5，不是生产真实库样本，不覆盖真实 CUD 页面交互，不覆盖 R2 multipart，不覆盖 shadow-off/fallback，不覆盖完整 retirement ledger，也不解除 `apps/admin/server` 保护。

| 任务 taskId     | 路由清单行 routeInventoryLine | 规范路径 canonicalPath                                           | 方法 method | 归属模块 ownerModule | 浏览器证据 browserEvidence                                                                                                     | DB 或写入证据 dbWriteEvidence                                                      | fallback 证据 fallbackEvidence | 退役决策 retirementDecision | 删除候选 deleteCandidate | 备注 notes                                                            |
| --------------- | ----------------------------: | ---------------------------------------------------------------- | ----------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- | ------------------------------ | --------------------------- | ------------------------ | --------------------------------------------------------------------- |
| task540 partial |                            12 | `/api/dev-team/config-manage/center/list`                        | POST        | dev                  | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；已有低风险 center CUD 证据不能外推到本 overlay 的退役门禁 | pending                        | keep-source                 | false                    | 只补 list 页面 Network，不覆盖 center detail/create/update/delete     |
| task540 partial |                            17 | `/api/dev-team/config-manage/dictionary/list`                    | POST        | dev                  | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；真实 CUD 页面交互仍需单独验收                             | pending                        | keep-source                 | false                    | 只补 list 页面 Network，不覆盖 dictionary detail/create/update/delete |
| task540 partial |                            22 | `/api/dev-team/config-manage/item/list`                          | POST        | dev                  | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；真实 CUD 页面交互仍需单独验收                             | pending                        | keep-source                 | false                    | 只补 list 页面 Network，不覆盖 item detail/create/update/delete       |
| task540 partial |                            27 | `/api/dev-team/config-manage/type/list`                          | POST        | dev                  | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；真实 CUD 页面交互仍需单独验收                             | pending                        | keep-source                 | false                    | 只补 list 页面 Network，不覆盖 type detail/create/update/delete       |
| task540 partial |                           149 | `/api/setting-manage/system-manage/change-password/list`         | POST        | setting              | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；响应含人员字段，长期证据仅保存字段名和分页摘要            | pending                        | keep-source                 | false                    | 不覆盖 create/update/delete 或真实改密动作                            |
| task540 partial |                           153 | `/api/setting-manage/system-manage/community-configuration/list` | POST        | setting              | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；配置值已脱敏，不覆盖真实 CUD 页面交互                     | pending                        | keep-source                 | false                    | 不覆盖 create/update/delete 或配置写入闭环                            |
| task540 partial |                           157 | `/api/setting-manage/system-manage/initialize-cell/list`         | POST        | setting              | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；真实 CUD 页面交互仍需单独验收                             | pending                        | keep-source                 | false                    | 不覆盖 create/update/delete                                           |
| task540 partial |                           161 | `/api/setting-manage/system-manage/register-protocol/list`       | POST        | setting              | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；协议正文已脱敏，不覆盖真实 CUD 页面交互                   | pending                        | keep-source                 | false                    | 不覆盖协议 create/update/delete 或完整正文                            |
| task540 partial |                           165 | `/api/setting-manage/system-manage/system-config/list`           | POST        | setting              | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；系统配置值已脱敏，不覆盖真实 CUD 页面交互                 | pending                        | keep-source                 | false                    | 不覆盖 create/update/delete 或系统配置写入闭环                        |
