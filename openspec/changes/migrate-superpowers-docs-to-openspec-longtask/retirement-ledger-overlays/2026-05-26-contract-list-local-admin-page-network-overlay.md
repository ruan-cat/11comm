# 2026-05-26 contract-manage 5 个 list 本地页面 Network overlay

本 overlay 只补 `property-manage/contract-manage` 5 个有页面入口 list endpoint 的本地 admin H5 页面 Network 投影。证据 artifact 为 `.tmp/phase7-dev-browser/2026-05-26-contract-list-local-admin-page-network-evidence.md`。

采证环境：本地 `apps/api` 为 `http://127.0.0.1:3102`，本地 `apps/admin` 为 `http://127.0.0.1:8084`；admin 页面通过 `/api-shadow` proxy 转发到本地统一 `apps/api`。本 overlay 仅把对应行的 `browserEvidence` 从 pending 升级为本地页面 Network partial；所有行的 `retirementDecision` 仍为 `keep-source`，`deleteCandidate=false`。

关闭边界：本 overlay 不是生产 admin H5、不是生产 `DB_READY`、不覆盖真实 CUD 页面交互、不覆盖 R2 multipart、不覆盖 shadow-off/fallback、不覆盖完整 retirement ledger，也不解除 `apps/admin/server` 保护。

| 任务 taskId             | 路由清单行 routeInventoryLine | 规范路径 canonicalPath                                     | 方法 method | 归属模块 ownerModule | 浏览器证据 browserEvidence                                                                                                     | DB 或写入证据 dbWriteEvidence                                                                             | fallback 证据 fallbackEvidence | 退役决策 retirementDecision | 删除候选 deleteCandidate | 备注 notes                                                 |
| ----------------------- | ----------------------------: | ---------------------------------------------------------- | ----------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- | ------------------------------ | --------------------------- | ------------------------ | ---------------------------------------------------------- |
| task540/task588 partial |                            58 | `/api/property-manage/contract-manage/change/list`         | POST        | contract             | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；`change/list` 生产 CUD baseline 仍受 task101 阻断，不能写成 DB_READY 或 residual | pending                        | keep-source                 | false                    | 只补 list 页面 Network，不覆盖 `change` CUD/detail         |
| task540/task588 partial |                            64 | `/api/property-manage/contract-manage/draft-contract/list` | POST        | contract             | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；不覆盖 `draft-contract` CUD/detail 的写入、回滚或 residual                       | pending                        | keep-source                 | false                    | 只补 list 页面 Network，不覆盖 `draft-contract` CUD/detail |
| task540/task588 partial |                            66 | `/api/property-manage/contract-manage/expire/list`         | POST        | contract             | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；生产真实库样本和全局 DB_READY 不由本 overlay 关闭                                | pending                        | keep-source                 | false                    | 只补 list 页面 Network                                     |
| task540/task588 partial |                            67 | `/api/property-manage/contract-manage/first-party/list`    | POST        | contract             | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；生产真实库样本和全局 DB_READY 不由本 overlay 关闭                                | pending                        | keep-source                 | false                    | 只补 list 页面 Network                                     |
| task540/task588 partial |                            72 | `/api/property-manage/contract-manage/type/list`           | POST        | contract             | 本地 admin H5 页面 Network 已补，见本 overlay 证据 artifact；页面 URL 为 `8084/api-shadow/...`，通过 proxy 命中本地 `apps/api` | list 只读页面响应为 200；生产真实库样本和全局 DB_READY 不由本 overlay 关闭                                | pending                        | keep-source                 | false                    | 只补 list 页面 Network                                     |
